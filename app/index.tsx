/**
 * صفحه اصلی اپ — نقطه ورودی expo-router بعد از loading.
 */

import React from 'react';
import { MainPanel } from '../src/features/ui/MainPanel';

export default function HomeScreen() {
  return <MainPanel />;
}
